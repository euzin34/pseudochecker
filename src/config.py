#
# Application configuration. Premium features hook in via TIER later.

TIER = "free"

# When False, warnings are reported but do not fail validation (web default).
TREAT_WARNINGS_AS_FAILURE = False

# Enable Python preview generation after successful parse.
ENABLE_PYTHON_PREVIEW = True
